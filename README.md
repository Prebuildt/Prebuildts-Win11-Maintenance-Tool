It must be ran as an admin. right click the "Run As Admin.bat" and select run as administrator. I did not implement elevation.

Process: SFC > DISMRestoreHealth > SFC Again > ClearTempFiles > FlushDNS > DriveOptimization (Trim/defrag) > DISMComponentCleanup (Update Leftovers) > DriverStoreCleanup > WindowsUpdateReset > Store, Icon & Thumbnail Cache Reset > CheckDiskHealth (chkdsk)
